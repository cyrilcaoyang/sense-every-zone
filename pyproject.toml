[build-system]
requires = ["setuptools >= 61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sense_every_zone"
version = "0.2.0"
description = "Environmental sensor nodes for the AC Organic Self-Driving Lab — STATUS_SPEC v1.2"
readme = "README.md"
license = {file = "LICENSE"}
requires-python = ">=3.10"
authors = [
    {name = "Yang Cao", email = "yangcyril.cao@utoronto.ca"}
]
dependencies = [
    "PyYAML>=6.0",
    "smbus2",
    "sdl-lab-contract",
]

[project.optional-dependencies]
# FastAPI service layer
api = [
    "fastapi>=0.111",
    "uvicorn[standard]>=0.29",
    "pydantic>=2.0",
]
# Raspberry Pi hardware drivers
pi = [
    "adafruit-blinka",
    "adafruit-circuitpython-ads1x15",
    # SEN55 uses raw smbus2 transactions — no Sensirion SDK needed
]
dev = [
    "pytest",
    "pytest-cov",
    "pytest-asyncio",
    "httpx",
    "black",
    "flake8",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv.sources]
sdl-lab-contract = { git = "https://github.com/AccelerationConsortium/sdl-lab-contract", tag = "v1.2.0" }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra -q"
asyncio_mode = "auto"
norecursedirs = ["docs", "*.egg-info"]
